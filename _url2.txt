10: Within the feasibility regime $N\tau \leq T_{\mathrm{val}}$, TDMA-CBAA inherits CBAA's core consensus promise: termination in $T_{ideal} + N\tau K$ and a conflict-free final assignment. Solution quality for fixed-score episodes is characterized by a conditional optimality statement (Prop.~\ref{prop:optimality}) rather than an unconditional 50\% bound.
4: The synchronization--staleness phase transition (Thm.~\ref{thm:noiseless}(ii)) elevates a routine engineering parameter---how long to remember a teammate's telemetry---into a first-class scalability constraint.
8: Equally instructive is what does \emph{not} degrade: per-link PDR follows the analytic channel model independently of swarm logic (Sec.~\ref{sec:results-pdr}), and cohesion remains stable across radio conditions once assignments exist.
12: Our results refine, rather than contradict, CBBA theory~\cite{choi2009}: under bounded-delay communication whose bound respects belief expiry, convergence carries over (Thm.~\ref{thm:noiseless}) and the belief-expiry process admits an exact reliability bound (Thm.~\ref{thm:probabilistic}); solution-quality guarantees are treated conditionally (Prop.~\ref{prop:optimality}).
23: Proposition~\ref{prop:optimality} is validated structurally (conflict-freeness at consensus); we do not yet report realized utility against a Hungarian-optimal upper bound, so its utility-comparison claim remains to be quantified empirically.
33: First, \emph{adaptive horizons}: let each agent set $T_{\mathrm{val},i} = \hat{c}\,\widehat{N}\tau$ from its estimated neighbor count, converting Theorem~\ref{thm:noiseless}(ii)'s constraint into a self-scaling protocol.
34: Second, \emph{utility-grounded evaluation}: add an optimal-assignment oracle to measure realized optimality ratios and empirically quantify the utility gap relative to the optimal assignment that Proposition~\ref{prop:optimality} frames.
10: Belief validity is fixed at $T_{\mathrm{val}} = 2.0$\,s throughout; by Theorem~\ref{thm:impossibility}, configurations with $N\tau > T_{\mathrm{val}}$ lie in the collapsed regime for \emph{verified} consensus---first-passage agreement may still occur transiently there, which is precisely the distinction our metrics are designed to expose.
22: The dataset analyzed in Section~\ref{sec:results} uses $t = 15$ trials per configuration ($324 \times 15 = 4{,}860$ runs), tripling the replication of the earlier five-trial release to give statistically stable within-cell estimates; the identical harness supports arbitrary trial counts via a single command-line flag.
24: Because $T_{\mathrm{val}} = 2$\,s and $\tau \leq 100$\,ms, configurations with $n \geq 21$ at $\tau = 100$\,ms ($N\tau > 2$\,s) fall beyond the staleness threshold of Theorem~\ref{thm:noiseless}; this lets us probe both sides of the verified-consensus boundary---and, critically, dissociate transient first-passage agreement from persistent consensus---within one matrix.
50: The estimand is deliberately conditional: because a transmission that is lost at the sender never generates offered links, PDR measures $P(\text{decode} \mid \text{transmission occurred}) = 1 - p_{\mathrm{rx}}$, not the joint end-to-end success $q = (1-p_{\mathrm{tx}})(1-p_{\mathrm{rx}})$ used in the theory of Sec.~\ref{sec:theory}. Transmission-side success is tracked separately as $P_{\mathrm{tx}} = \text{packets\_sent} / \text{scheduled opportunities}$, so the end-to-end quantity is recovered exactly as $P_{\mathrm{e2e}} = P_{\mathrm{tx}} \cdot \mathrm{PDR}$. All three quantities are recorded per trial.
52: \emph{(4) Mean Time to Consensus (MTC)}---wall-clock time from trial start to global ledger agreement (Def.~\ref{def:firstpassage}), censored at the horizon:
57: In addition to first-passage MTC we record a binary \emph{verified-consensus} indicator (agreement persisting for $T_{\mathrm{val}}/\Delta t$ frames after first passage, Def.~\ref{def:verified}) and the number of post-passage \emph{regressions} caused by belief expiry; Sec.~\ref{sec:results} reports both because Theorem~\ref{thm:noiseless} makes sharply different predictions for the two quantities.
67: Comparisons against all four baselines follow the protocol of Sec.~\ref{sec:test-matrix}; headline statistics in Sec.~\ref{sec:results} are reported on the released TDMA-CBAA corpus.
70: We make the statistical unit explicit throughout. Each \emph{configuration} fixes one combination of the five factors (Section~\ref{sec:test-matrix}); each configuration is instantiated over $t = 15$ \emph{independent seeds}. The independent observation is the seed-level trial; aggregate figures across configurations are descriptive, not additional replication. Distinguishing \emph{runs} ($4{,}860$), \emph{seeds} ($15$ per configuration), and \emph{conditions} ($324$) is essential to avoid inflating apparent sample size.
72: All metrics reject normality (Shapiro--Wilk, Sec.~\ref{sec:results}); we therefore:
30: \item \textbf{Convergence theory under scheduled, lossy communication (Thm.~\ref{thm:noiseless}).}
33: for $K$ auction rounds, provided one full TDMA frame fits within the belief-validity horizon, $N\tau \leq T_{\mathrm{val}}$ (Thm.~\ref{thm:noiseless}(i)).
35: \item \textbf{Single-task consensus and a conditional optimality statement (Prop.~\ref{prop:optimality}).}
37: \item \textbf{A synchronization--staleness phase transition (Sec.~\ref{sec:theory-noiseless}(ii), Thm.~\ref{thm:noiseless}).}
4: Algorithm~\ref{alg:tdmacbaa} summarizes one simulation frame; the components relative to canonical CBBA---slotted medium access, two-stage packet loss, and belief expiry---are detailed in Sections~\ref{sec:method-slotted}--\ref{sec:method-belief}, followed by the D-TDMA extension in Section~\ref{sec:dtdma}.
42: This mechanism provides fault tolerance against departed agents, but as Section~\ref{sec:theory-noiseless}(ii) shows, it is also the mechanism through which belief expiry can destabilize consensus.
61: An allocation-only ablation (Sec.~\ref{sec:setup-ablation}) with motion disabled confirms that MTC ordering across baselines is driven by the communication layer.
66: This gap is precisely the quantity that couples to the belief horizon $T_{\mathrm{val}}$ in Section~\ref{sec:theory}: whenever the gap exceeds $T_{\mathrm{val}}$, the freshest belief about that peer expires before it can be renewed.
79: Its interaction with TDMA scheduling is the central object of Section~\ref{sec:theory}: expiry converts bounded delay into bounded \emph{belief age}, and consensus survives only while worst-case belief age stays below the horizon.
95: \item Grants are non-preemptive and preserve round-robin order: at most one doubling occurs per visit, slot order is never permuted, so the frame remains collision-free and the analysis of Section~\ref{sec:theory} carries over with effective cycle time bounded by $2N\tau$.
98: D-TDMA thus adds an adaptive, safety-aware layer on top of deterministic medium access: contested agents obtain up to twice the telemetry bandwidth exactly when their state estimates matter most (threat evasion, terminal approach), while Theorem~\ref{thm:noiseless}(i) and the conditional optimality statement of Proposition~\ref{prop:optimality} carry over under the relaxed condition $2N\tau \leq T_{\mathrm{val}}$.
3: We consider a team of $N$ homogeneous drones required to allocate themselves to a set of geographically distributed objectives in a bounded planar workspace, under a contention-limited radio channel. Table~\ref{tab:notation} summarizes the notation.
68: These degenerate cases define the four radio baselines evaluated in Section~\ref{sec:setup}.
76: where $c_{ij} = \max(0.1,\; C_0 - d(\mathbf{p}_i, \mathbf{q}_j))$ is drone $i$'s bid for objective $o_j$ ($C_0 = 100$), subject to (a) each objective receiving at most one winner, (b) convergence to ledger agreement within the mission horizon, and (c) operation over the lossy slotted channel of Section~\ref{sec:comm-model}.
85: We distinguish the \emph{first-passage} instance of Eq.~\eqref{eq:consensus-def} (Def.~\ref{def:firstpassage}) from \emph{verified} consensus, in which Eq.~\eqref{eq:consensus-def} persists for at least $T_{\mathrm{val}}$ (Def.~\ref{def:verified}).
3: We report results from the released corpus of $4{,}860$ TDMA-CBAA trials ($324$ configurations, $15$ independent seeds each; Sec.~\ref{sec:test-matrix}) together with the four-baseline ablation sweep ($900$ runs) and the communication--motion isolation ablation of Sec.~\ref{sec:setup-ablation}.
7: Table~\ref{tab:headline} summarizes the four metrics across the full corpus.
8: Every metric rejects normality (Shapiro--Wilk, all $p < 10^{-4}$, $n = 4860$), justifying the non-parametric and survival treatments of Sec.~\ref{sec:setup-stats}.
28: The aggregate PDR of $0.827$ is consistent with the two-stage loss model of Sec.~\ref{sec:method-loss}: averaging the per-loss predictions $1.0,\ 0.926,\ 0.786,\ 0.595$ over the corpus's loss mixture reproduces $0.827$, and per-condition errors are below $10^{-3}$ (Sec.~\ref{sec:results-pdr}), providing an end-to-end sanity check that the radio simulator behaves as designed.
31: Fig.~\ref{fig:mtc_vs_swarm} shows MTC against swarm size.
32: Consensus time grows approximately linearly with $n$ at low loss, consistent with the scheduling-delay term $N\tau K$ in Theorem~\ref{thm:noiseless}: each additional drone both lengthens the frame and adds one ledger to synchronize.
33: At larger swarms and higher loss rates the curve separates into a converging branch and a saturated branch pinned at the 50\,s horizon---the two regimes delimited by Theorems~\ref{thm:noiseless} and \ref{thm:impossibility}, with Theorem~\ref{thm:probabilistic} bounding the expiry-free probability of the deterministic precondition.
38: \caption{Mean time to consensus vs.\ swarm size (error bars: std.\ across trials). Beyond the staleness threshold $N\tau > T_{\mathrm{val}} = 2$\,s of Thm.~\ref{thm:noiseless}(ii), MTC saturates at the mission horizon, marking the synchronization--staleness phase transition.}
42: The convergence CDF (Fig.~\ref{fig:convergence_curve}) makes this bimodality explicit: the empirical distribution exhibits a steep rise at short times (sub-threshold trials) followed by a long flat plateau (censored trials above threshold), rather than the unimodal tail a purely stochastic slowdown would produce.
47: \caption{Empirical CDF of time to consensus. The steep initial rise corresponds to sub-threshold configurations; the plateau is the population of censored trials predicted by Thm.~\ref{thm:noiseless}(ii).}
52: Fig.~\ref{fig:pdr_vs_pl} shows PDR degrading gracefully with nominal transmit-leg loss.
55: by the two-stage model of Sec.~\ref{sec:method-loss}: with the revised
70: The heatmap (Fig.~\ref{fig:heatmap}) locates the phase transition of Theorem~\ref{thm:noiseless}(ii) in parameter space: mean MTC rises smoothly in the low-$n$, low-loss corner, then jumps discontinuously toward the horizon cap as $N\tau$ approaches and exceeds $T_{\mathrm{val}} = 2$\,s---the predicted boundary running diagonally through the $(n, p)$ plane.
75: \caption{Mean MTC over swarm size $\times$ packet loss, annotated with convergence rate. The jump toward the cap traces the synchronization--staleness phase transition of Thm.~\ref{thm:noiseless}(ii).}
81: Fig.~\ref{fig:survival} shows Kaplan--Meier survivor curves per baseline: ideal and loss-only open-channel swarms converge almost immediately (median $0.2$--$0.4$\,s), TDMA-only converges with a moderate tail (median $3.3$\,s), while TDMA-with-loss retains a $64.9\%$ convergence fraction (median $6.4$\,s), its survivor curve never reaching the baseline---the signature of the structural phase transition rather than a slow-but-certain process. Pairwise log-rank tests and a TDMA-only Cox proportional-hazards fit (staleness ratio and packet loss as covariates, robust standard errors) supplement the curves (Sec.~\ref{sec:setup-stats}); full outputs are in \texttt{survival\_analysis.txt}.
91: Within TDMA conditions we use regression as a descriptive mechanism analysis rather than a causal estimator (Eq.~\eqref{eq:regression}, Sec.~\ref{sec:setup-stats}): the staleness ratio $\rho = N\tau/T_{\mathrm{val}}$ and the packet-loss rate are treated as controlled experimental factors, and HC3 robust standard errors are reported. The estimated interaction $\beta_3$ between $\rho$ and $p_{\mathrm{tx}}$ is consistent with the joint scheduling-and-loss mechanism of Thm.~\ref{thm:noiseless}(ii); full coefficient tables are in \texttt{regression\_analysis.txt}.
93: Table~\ref{tab:ablation} summarizes the four-baseline sweep, which refines the causal decomposition of Sec.~\ref{sec:baselines}: moving from ideal (MTC $0.34$\,s) to TDMA-only raises MTC to
96: Because the ablation holds the topology fixed ($M{=}5$, $k{=}2$, $\tau{=}50$\,ms) and varies only $N \times p_{\mathrm{tx}}$ with 30 seeds per condition, whereas Table~\ref{tab:headline} aggregates the full 324-configuration factorial at 15 seeds, the two tables' absolute values are not directly comparable; the differing sampling explains the modest shift in TDMA-with-loss MTC ($22.4$\,s here vs.\ $21.6$\,s corpus mean) while the qualitative ordering across baselines is preserved.
114: Finally, we run the communication--motion isolation ablation of Sec.~\ref{sec:setup-ablation} (motion disabled, all four channel models) to separate auction-communication effects from stale flocking/collision state; the per-configuration results are released with the raw data and indicate that the MTC ordering across baselines is preserved when motion is disabled, confirming the communication layer as the dominant driver of the reported differences.
4: Section~\ref{sec:theory-age} defines the \emph{belief-age process}---the exact quantity whose interplay with scheduling produces both the guarantees and the failure mode.
5: Sections~\ref{sec:theory-noiseless}--\ref{sec:theory-stoch} then state what does and does not survive: consensus definitions made precise, a deterministic guarantee in the noiseless regime, and an exact stochastic reliability bound that invokes no independence assumptions between overlapping loss windows.
6: Section~\ref{sec:theory-complexity} analyzes complexity.
8: Throughout, $q = (1-p_{\mathrm{tx}})(1-p_{\mathrm{rx}})$ denotes the per-frame end-to-end delivery success probability on a link, and all probabilistic statements are with respect to the independent loss process of Sec.~\ref{sec:comm-model}.
40: Two per-link quantities follow immediately from Definitions~\ref{def:gap}--\ref{def:immunity}:
49: The D-TDMA extension of Sec.~\ref{sec:dtdma}, by stretching granted slots to $2\tau$, at most doubles the effective frame period and therefore \emph{halves} the immunity available near threshold.
71: The distinction is decisive for interpreting Sec.~\ref{sec:results}: a swarm may agree momentarily ``in passing'' while being structurally incapable of \emph{holding} agreement.
74: The gossip update rule of Algorithm~\ref{alg:tdmacbaa} is monotone in the following sense. For each objective $o_j$, the bid entry stored by agent $d_i$ is nondecreasing over time and equals the maximum of all incoming bids for $o_j$ that $d_i$ has decoded, provided no stored entry is pruned by belief expiry:
83: The receive handler adopts a sender's bid if and only if it is strictly larger than the local entry, and otherwise leaves the local entry unchanged (Sec.~\ref{sec:method-belief}); winner indices travel with the adopted bid, so the stored pair $(\mathrm{winner}, \mathrm{bid})$ changes only out of a strictly smaller stored bid. Because scores are constant throughout the episode (the frozen-score setting of Prop.~\ref{prop:optimality}), a larger received value can be overwritten only upward, never downward, whenever no pruning event removes it. Repeated application over the sequence of decoded messages yields exactly the running maximum of Eq.~\eqref{eq:monotone-merge}.
87: Suppose $p_{\mathrm{tx}} = p_{\mathrm{rx}} = 0$ (lossless TDMA), so every belief refreshes exactly once per frame period $N\tau$, and let scores be frozen for the episode (the frozen-score setting of Prop.~\ref{prop:optimality}). Then:
98: If $N\tau > T_{\mathrm{val}}$, every refresh arrives already expired: at decode time the sender entry is older than $T_{\mathrm{val}}$, so ghost pruning removes its wins before they influence the ledger. First-passage agreement may still occur transiently---and empirically does (Sec.~\ref{sec:results})---but it cannot persist: between two consecutive refreshes of any peer, that peer's entries expire and are pruned, forcing disagreement within every frame period. Verified consensus therefore has probability zero.
103: (i) Under lossless TDMA each decoded packet has age at most $N\tau \leq T_{\mathrm{val}}$, so pruning never fires; each agent's ledger then evolves by the monotone merge of Lemma~\ref{lem:monotone} under a fixed serialization of exactly the all-to-all message pattern per round. Convergence follows from the standard monotone-auction argument~\cite{choi2009}; each serialized round can lag the ideal round by at most one frame period $N\tau$, giving Eq.~\eqref{eq:ttdma-bound}, and with no expiry the terminal agreement never breaks, establishing verified consensus.
111: Consider the communication model of Sec.~\ref{sec:comm-model}
114: Sec.~\ref{sec:method-auction}. If
124: Def.~\ref{def:eventual} cannot be maintained for the full swarm
147: by Def.~\ref{def:eventual}. Packet loss cannot improve this condition,
153: With stochastic loss, refreshes arrive with geometrically distributed gaps (Def.~\ref{def:gap}) and expiry becomes a random event. Two errors must be avoided: treating overlapping loss windows as independent (they share samples), and claiming deterministic delay bounds that stochastic failure runs violate with nonzero probability. We make neither: the single-link probability below is \emph{exact}, and the network-level statement uses only the union bound, hence remains valid under arbitrary correlation in the loss process.
185: Under the independence assumptions of Sec.~\ref{sec:comm-model},
241: Table~\ref{tab:complexity} compares per-agent costs ($n = |\mathcal{D}|$, $m = |\mathcal{O}|$); one frame consumes $\lceil N\tau / \Delta t \rceil$ ticks.
263: TDMA adds $O(n)$ belief filtering per tick but leaves auction compute at $O(m)$; per-agent communication drops to one broadcast per frame while wall-clock per round inflates to $N\tau$---the scheduling-delay term in Theorem~\ref{thm:noiseless}.
